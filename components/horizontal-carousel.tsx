import { ThemedView } from "./themed-view";
import { Typography } from "./ui/typography";

export default function HorizontalCarousel() {
  return (
    <ThemedView>
      <Typography variant="h1">HorizontalCarousel</Typography>
    </ThemedView>
  );
}
